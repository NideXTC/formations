var tab = [4,3,5,8];

console.log(tab); // affiche toutes les valeurs 
console.log(tab[0]); // affiche la première valeur 
console.log(tab.length); // affiche la taille du tableau

console.info(typeof tab); // object
